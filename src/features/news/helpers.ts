// import { NEWSDATA_UPD_INTERVAL } from '@/features/news/constants';
// import { NewsDataArticle } from '@/features/news/types';

// export function allowNewsDataArticlesUpdate(
//   newsDataArticles: NewsDataArticle[] | null,
//   newsDataTimestamp: number | null
// ) {
//   if (!newsDataArticles || !newsDataTimestamp) return true;
//   return Date.now() - newsDataTimestamp > NEWSDATA_UPD_INTERVAL;
// }
